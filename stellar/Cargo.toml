[workspace]
members = ["contracts/drip_stream"]
resolver = "2"

# Soroban-recommended release profile for optimised wasm output.
[profile.release]
opt-level = "z"
overflow-checks = true
debug = 0
strip = "symbols"
debug-assertions = false
panic = "abort"
codegen-units = 1
lto = true
